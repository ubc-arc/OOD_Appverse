---
# Set cluster to the id matching your /etc/ood/config/clusters.d/<cluster>.yml
cluster: "mycluster"
form:
  - version
  - auto_accounts
  - bc_num_hours
  - auto_queues
  - bc_num_slots
  - mem_per_cpu
  - bc_email_on_started
attributes:
  bc_num_hours:
    widget: number_field
    min: 1
    max: 24
    step: 1
    value: 1
    label: "Wall time (hours)"
    help: "Maximum walltime for the session (hours)."
    required: true
  bc_num_slots:
    widget: number_field
    label: "Number of cores"
    value: 1
    min: 1
    max: 64
    step: 1
    help: "Number of CPU cores to request."
    required: true
  mem_per_cpu:
    widget: number_field
    label: "Memory per core (GB)"
    value: 4
    min: 1
    max: 64
    step: 1
    help: "Amount of memory per core in GB."
    required: true
  version:
    widget: select
    label: "UGENE version"
    help: "Select the version of UGENE to launch."
    options:
      - ["UGENE 53.1", "/path/to/apptainer_images/ugene/ugene_53.1.sif"]
