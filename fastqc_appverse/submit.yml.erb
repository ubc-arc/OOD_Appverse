<%-
  cores = num_cores.to_i
  slurm_args = ["--nodes", "1", "--ntasks-per-node", "#{cores}", "--mem-per-cpu", "4G"]
-%>
---
batch_connect:
  template: "vnc"
script:
  native:
  <%- slurm_args.each do |arg| %>
  - "<%= arg %>"
  <%- end %>