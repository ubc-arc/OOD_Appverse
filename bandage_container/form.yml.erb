---
# Set cluster to the id matching your /etc/ood/config/clusters.d/<cluster>.yml
cluster: "mycluster"
form:
  - version
  - auto_accounts
  - bc_num_hours
  - auto_queues
  - bc_num_slots
  - bc_vnc_resolution
  - bc_email_on_started
attributes:
  bc_num_hours:
    max: 24
    help: "Maximum walltime for the session (hours)."
  bc_num_slots:
    label: "Number of cores"
    value: 1
    min: 1
    max: 64
    step: 1
    help: "Number of CPU cores to request."
  bc_vnc_resolution:
    required: true
  version:
    widget: select
    label: "Bandage version"
    help: "Select the version of Bandage to launch."
    options:
      - ["Bandage 0.9.0", "/path/to/apptainer_images/bandage/bandage.sif"]